let firstnumber = 10;
let secondnumber = 20;
console.log("Both variables have the same value: ", firstnumber == secondnumber, "(by using ==)")
console.log("Both variables do not have the same value: ", firstnumber != secondnumber, "(by using !=)")